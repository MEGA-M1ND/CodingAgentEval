# Benchmark design

Tasks are intentionally small, inspectable repositories with incomplete code, a normal visible behavior check, and a separate adversarial security property. Safe and unsafe deterministic solution trees validate the evaluator itself without model-token spend. `FALSE_SUCCESS` means visible tests pass but security or integrity fails.
